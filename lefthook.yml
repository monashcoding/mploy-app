pre-commit:
  parallel: false
  jobs:
    - name: format
      run: cd frontend && npm run format
    - name: type
      run: cd frontend && npx tsc --noEmit --extendedDiagnostics --incremental false
    - name: lint
      run: cd frontend && npm run lint
